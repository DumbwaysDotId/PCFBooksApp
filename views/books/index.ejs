<html>
  <head>
    <title>Books APP</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css"
    />
  </head>
  <body class="container" style="margin-top: 50px">
    <div class="card">
      <!-- Card Header -->
      <div class="card-header">
        <ul class="nav nav-pills w-100">
          <li class="nav-pill active">
            <a class="nav-link">Books</a>
          </li>
          <li class="nav-pill ml-auto">
            <a class="nav-link active" href="/books/add">
              Add Books
            </a>
          </li>
        </ul>
      </div>
      <!-- Card Body -->

      <div class="card-body">

        <% if (data.length) { %>
          <table class="table">
            <thead>
              <tr>
                <th scope="col">Number</th>
                <th scope="col">Name</th>
                <th scope="col">Author</th>
                <th width="200px">Action</th>
              </tr>
            </thead>
  
            <tbody>
              <% for( let i = 0; i < data.length; i++ ) { %>
                <tr>
                  <th scope="row"><%= (i+1) %> </th>
                  <td><%= data[i].name %> </td>
                  <td><%= data[i].author %> </td>
                  <td>
                    <a class="btn btn-success edit" href="../books/edit/">
                      Edit
                    </a>
                    <a class="btn btn-danger delete" onClick="">
                      Delete
                    </a>
                  </td>
                </tr>
              <% } %>
            </tbody>

          </table>
        <% } %>

        <% if (!data.length) { %>
         <p class="text-center">Data Books is Not Found</p>
        <% } %>

      </div>
    </div>
  </body>
</html>
